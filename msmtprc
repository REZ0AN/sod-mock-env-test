defaults
auth           on
tls            on
tls_trust_file /etc/ssl/certs/ca-certificates.crt
logfile        /var/log/msmtp.log

account        SOURCE_NAME
host           MAIL_HOST
port           MAIL_PORT
from           MAIL_FROM
user           MAIL_USER
password       MAIL_PASSWORD

account default : SOURCE_NAME